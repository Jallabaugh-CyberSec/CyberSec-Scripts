# CyberSec-Scripts
A collection of scripts for cybersecurity tasks.

- `siem_query.sql`: SQL query to analyze failed logins in a SIEM tool (e.g., Splunk).
- `log_parser.java`: Java script to parse logs and flag failed login attempts.
